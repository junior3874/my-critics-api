import { UserLoadByTokenUseCase } from "./userLoadByTokenUseCase";

describe("#userLoadByTokenUseCase", () => {
  const makeSut = () => {
    // private readonly decrypter: Decrypter,
    // private readonly loadAccountByTokenRepository: LoadAccountByTokenRepository
    const sut = new UserLoadByTokenUseCase();
  };
  test("should load user by token with sucess", async () => {});
});
